models:
  text:
    provider: openai
    model: gpt-4o-mini
    api_key: "sk-your-api-key"
    # base_url: "https://your-endpoint/v1"  # 可选，使用第三方 API 时填写
  vision:
    provider: openai
    model: gpt-4o
    api_key: "sk-your-api-key"
    # base_url: "https://your-endpoint/v1"  # 可选，使用第三方 API 时填写
